{
  "name": "poc-queue-workers",
  "version": "1.0.0",
  "description": "Send SQS message",
	"type": "module",
  "main": "send.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.1095.0",
    "dotenv": "^16.0.0"
  }
}
